# Graph Schema

No graph schema is implemented in this bootstrap.

When graph work begins, relationships must use explicit Go types, stable
deterministic identifiers, and preserved evidence. The graph should remain
in-memory until a concrete task requires otherwise.

This document is intentionally limited to constraints so the bootstrap does not
predefine untested domain logic.
